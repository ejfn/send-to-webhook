{
  "name": "send-to-webhook",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist/ && tsc && cp -r public/. dist/",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/chrome": "^0.0.268"
  }
}